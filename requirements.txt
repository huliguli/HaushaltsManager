# HaushaltsManager — runtime + build dependencies (pinned)
# Verified installed wheels on Python 3.14 (win_amd64). Re-pin with `pip freeze` after upgrades.

# --- GUI ---
PyQt6==6.11.0
PyQt6-Qt6==6.11.1
PyQt6-sip==13.11.1

# --- Spreadsheets ---
openpyxl==3.1.5

# --- PDF: read (import) + write (reports) ---
PyMuPDF==1.27.2.3
reportlab==5.0.0

# --- Charts ---
matplotlib==3.11.0

# --- Packaging (build-time only) ---
pyinstaller==6.16.0

# --- Tests (dev only) ---
pytest==8.4.2
